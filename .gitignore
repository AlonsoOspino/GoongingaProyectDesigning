node_modules/
.env
.env.*
!.env.example
!.env.sample
!.env.template

# Credentials and certificates
*.pem
*.key
*.p12
*.pfx
*.crt
*service-account*.json
*credentials*.json

# Local databases/backups
*.db
*.sqlite
*.sqlite3
*.sqlitedb
backups/

# Local AI agent, skill, and design-review tooling
.agents/
.codex/
.impeccable/
skills-lock.json

# Deploy configuration with the production server address (template is tracked)
deploy.local.bat
!deploy.local.bat.example

# Next.js build output and salvaged/corrupt cache directories.
# A 297MB .next-corrupt-* cache was committed once; ".next/" alone did not catch it.
.next*/
*.tsbuildinfo

# Logs and scratch output
*.log
tmp/

# Local-only draft-table fixture used by /draft-table-dev
migration-uidesign/frontend/.draft-table-dev-data.json

# Subagent-driven development scratch (ledger, briefs, review packages)
.superpowers/
