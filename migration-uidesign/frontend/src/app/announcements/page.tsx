import { AnnouncementRenderer } from "@/announcements/AnnouncementRenderer";

export const metadata = { title: "Announcement Mode" };

export default function AnnouncementsPage() {
  return <AnnouncementRenderer standalone />;
}
